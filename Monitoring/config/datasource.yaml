apiVersion: v1


datasources:
  - name: PostgeSQL
    type: postgres
    access: proxy
    url: db.:5432
    database: test
    user: postgres
    secureJsonData:
      password: postgres
    JsonData:
      sslmode: disable